---
transition: slide-up
---

# 3. Tipos de Dados

O JavaScript é dinamicamente tipado, o que significa que você não precisa declarar o tipo explicitamente.

- **Primitivos**:
  - `String`: "Texto", 'Texto' ou `Texto com ${váriavel}` (Template Literals)
  - `Number`: 42, 3.14 (não há distinção entre int e float)
  - `Boolean`: `true` ou `false`
  - `null`: Representa a ausência intencional de valor.
  - `undefined`: Valor de uma variável declarada mas não inicializada.
- **Complexos**:
  - `Object`: `{ nome: "Paulo", idade: 30 }`
  - `Array`: `[1, 2, 3, "quatro"]`
  - `Function`: Blocos de código reutilizáveis.
