---
layout: default
---

# 🧑‍💻 Exercício de Sala: Sua Primeira Página


## Tags Obrigatórias

Sua página deve conter, no mínimo, as seguintes tags:

- **Estrutura Básica**: `<html>`, `<head>`, `<title>` e `<body>`
- **Títulos**: Pelo menos um `<h1>` e um `<h2>`
- **Textos**: Pelo menos dois parágrafos (`<p>`)
- **Listas**: Uma lista (`<ul>` ou `<ol>`) com itens (`<li>`)
- **Imagens**: Uma imagem (`<img>`)
- **Links**: Um link (`<a>`)
